{
  "name": "homework13",
  "version": "1.0.0",
  "description": "This project implements a basic **Express API** that mimics the functionality of `localStorage` for storing and retrieving data between requests. The data is stored persistently using a file-based approach, simulating the behavior of `localStorage` in a server-side environment.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.8.2",
    "bcryptjs": "^3.0.2",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongoose": "^8.12.1"
  }
}
